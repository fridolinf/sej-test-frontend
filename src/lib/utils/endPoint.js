const BASEURL = "https://asia-southeast2-sejutacita-app.cloudfunctions.net";

const ENDPOINT = {
  categories: "/fee-assessment-categories",
  book: "/fee-assessment-books",
};

const api = {
  BASEURL: BASEURL,
  ENDPOINT: ENDPOINT,
};

export default api;
